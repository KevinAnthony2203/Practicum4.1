import '../css/app.scss'

// Resto de importaciones de JavaScript aquí
import './bootstrap';

// Configuración global
document.addEventListener('DOMContentLoaded', () => {
    // Inicializaciones globales aquí
});
